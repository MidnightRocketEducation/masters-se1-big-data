apiVersion: v1
kind: Service
metadata:
  name: world-clock-test-producer-svc
spec:
  type: ClusterIP
  selector:
    app: world-clock-test-producer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
